<Window x:Class="PsychiatricHospitalWPF.Views.Prescriptions.ExecutePrescriptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Отметка выполнения назначения" 
        Height="520" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- шапка -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <StackPanel>
                <TextBlock Text="Отметка выполнения назначения" 
                          FontSize="16" 
                          FontWeight="Bold" 
                          Foreground="White"/>
                <TextBlock x:Name="txtPrescriptionInfo" 
                          Text="" 
                          FontSize="12" 
                          Foreground="White"
                          Opacity="0.9"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- форма -->
        <StackPanel Grid.Row="1" Margin="20">

            <!-- дата выполнения -->
            <Label Content="Дата выполнения:*" FontWeight="SemiBold"/>
            <DatePicker x:Name="dpExecutionDate" 
                       Height="35"
                       Padding="8"
                       Margin="0,0,0,15"
                       DisplayDateEnd="{x:Static sys:DateTime.Now}"/>

            <!-- время выполнения -->
            <Label Content="Время выполнения (ЧЧ:ММ):*" FontWeight="SemiBold"/>
            <TextBox x:Name="txtExecutionTime" 
                    Height="35"
                    Padding="8"
                    Margin="0,0,0,5"
                    TextChanged="TxtExecutionTime_TextChanged"/>
            <TextBlock x:Name="lblTimeHint" 
                      Text="Например: 14:30"
                      FontSize="11"
                      Foreground="Gray"
                      Margin="5,0,0,15"/>

            <!-- примечания -->
            <Label Content="Примечания:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtNotes" 
                    Height="80"
                    Padding="8"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    VerticalScrollBarVisibility="Auto"
                    Margin="0,0,0,5"/>
            <TextBlock Text="Например: Пациент отказался, Выполнено частично"
                      FontSize="11"
                      Foreground="Gray"
                      Margin="5,0,0,0"/>

        </StackPanel>

        <!-- кнопки -->
        <Border Grid.Row="2" Background="#f5f5f5" Padding="15" BorderBrush="#ddd" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="lblError" 
                          Grid.Column="0"
                          Text=""
                          Foreground="Red"
                          VerticalAlignment="Center"
                          TextWrapping="Wrap"
                          Visibility="Collapsed"/>

                <Button x:Name="btnExecute" 
                       Content="✓ Отметить выполнение" 
                       Grid.Column="1"
                       Click="BtnExecute_Click"
                       Background="#4CAF50"
                       Foreground="White"
                       Padding="20,8"
                       Margin="5,0"
                       Cursor="Hand"/>

                <Button x:Name="btnCancel" 
                       Content="✗ Отмена" 
                       Grid.Column="2"
                       Click="BtnCancel_Click"
                       Background="#757575"
                       Foreground="White"
                       Padding="20,8"
                       Margin="5,0"
                       Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>