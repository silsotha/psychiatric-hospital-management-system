<Window x:Class="PsychiatricHospitalWPF.Views.Prescriptions.CancelReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Причина отмены" 
        Height="320" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- шапка -->
        <Border Grid.Row="0" Background="#f44336" Padding="15">
            <TextBlock x:Name="txtTitle" 
                      Text="Укажите причину отмены назначения" 
                      FontSize="14" 
                      FontWeight="SemiBold" 
                      Foreground="White"/>
        </Border>

        <!-- форма -->
        <StackPanel Grid.Row="1" Margin="20">
            <Label Content="Причина:*" FontWeight="SemiBold"/>
            <TextBox x:Name="txtReason" 
                    Height="100"
                    Padding="10"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    VerticalScrollBarVisibility="Auto"
                    Margin="0,0,0,10"/>

            <TextBlock x:Name="lblCharCount" 
                      Text="Минимум 10 символов"
                      FontSize="11"
                      Foreground="Gray"
                      Margin="5,0,0,0"/>
        </StackPanel>

        <!-- кнопки -->
        <Border Grid.Row="2" Background="#f5f5f5" Padding="15" BorderBrush="#ddd" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnOk" 
                       Content="✓ Подтвердить" 
                       Click="BtnOk_Click"
                       Background="#f44336"
                       Foreground="White"
                       Padding="20,8"
                       Margin="0,0,10,0"
                       Cursor="Hand"/>

                <Button x:Name="btnCancel" 
                       Content="✗ Отмена" 
                       Click="BtnCancel_Click"
                       Background="#757575"
                       Foreground="White"
                       Padding="20,8"
                       Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
